#include <iostream>
#include <vector>
int bsr(int arr[],int s, int e ,int k){
    // bc
    if (s>e) return -1;
    }

// processing 
int mid=s+(e-s)/2;
if (arr[mid]==k) return mid;
if (arr[mid]<k) return 
if (arr[mid]>k)  return 


using namespace std;
int main () {
    
return 0;
  }